specVersion: "v1beta"
serviceId: "disney+ clone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "disney+ clone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
